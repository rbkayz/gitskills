# Web/API
NEXT_PUBLIC_REGISTRY_URL=http://localhost:3000
PUBLISH_TOKEN=change-me
ADMIN_TOKEN=change-me
# Optional: comma-separated GitHub logins that should be auto-granted admin role on sign-in.
# Example: ADMIN_GITHUB_LOGINS=rbkayz,another-admin
ADMIN_GITHUB_LOGINS=

# GitHub Login (Auth.js / NextAuth)
AUTH_SECRET=replace-with-random-secret
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Optional embedding connector for hybrid search
# OPENAI_API_KEY=
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Local storage for published tarballs (optional override)
# LOCAL_STORAGE_DIR=./apps/web/public/tarballs

# GCP Cloud Storage (optional production backend for published tarballs)
# If GCS_BUCKET is set, publish API uploads tarballs to GCS instead of local disk.
# GCP_PROJECT_ID=your-gcp-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json
# GCS_BUCKET=gitskills-artifacts
# GCS_PUBLIC_BASE_URL=https://storage.googleapis.com/gitskills-artifacts
# GCS_MAKE_PUBLIC=1

# Postgres (local)
DATABASE_URL=postgresql://gitskills:gitskills@localhost:5432/gitskills

# S3-compatible storage (local MinIO)
S3_ENDPOINT=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=minio
S3_SECRET_ACCESS_KEY=minio12345
S3_BUCKET=gitskills
